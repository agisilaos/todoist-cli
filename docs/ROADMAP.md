# Todoist CLI Roadmap

Planned/considered features:

- Quick-add via Sync API (full natural language parsing)
- NDJSON for all list outputs
- Task update by reference (fuzzy/ID)
- Workspace and collaboration commands
- OAuth device flow auth
- Rich output grouping for sections/projects
